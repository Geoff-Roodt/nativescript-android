android {
            defaultConfig {
                applicationId = "org.nativescript.GeoPhotoProjec"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }